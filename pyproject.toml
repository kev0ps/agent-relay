[project]
name = "agent-relay"
version = "0.1.0"
description = "Constrained MCP relay for explicitly enabled local capabilities"
readme = "README.md"
license = "MIT"
license-files = ["LICENSE"]
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.115,<1",
    "mcp>=2,<3",
    "pydantic>=2.8,<3",
    "pyyaml>=6,<7",
    "typing-extensions>=4.8,<5",
    "uvicorn>=0.30,<1",
    "websockets>=17,<18",
]

[project.optional-dependencies]
cua = ["cua-driver==0.19.3"]

[project.scripts]
agent-relay = "agent_relay.cli:main"

[dependency-groups]
dev = [
    "httpx2>=2.11,<3",
    "pytest>=8.0",
    "pytest-cov>=6,<8",
    "ruff>=0.5",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/agent_relay"]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = ["integration: real loopback network tests"]

[tool.ruff]
target-version = "py314"
line-length = 88

[tool.ruff.lint]
select = ["E", "F", "I"]
ignore = ["E501"]
